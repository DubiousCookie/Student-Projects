#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*int is simple numbers, no decimal points */
    int age = 30;
    /*doubles are decimal numbers, they allow decimal points*/
    double gpa = 4.0;
    /*char are characters only, only ONE CHARACTER*/
    char grade = 'A';
    /*While technically a Array, [] strings together characters into one phrase, will act differently then the stuff above*/
    char phrase [] = "Penguin Enclosure"

    return 0;
}
